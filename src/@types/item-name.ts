export interface ItemName {
  fallback: string; // Fallback text (e.g., "Crusher")
  translate: string; // Translation key (e.g., "wasd.machines.crusher")
  italic?: boolean; // Whether the text is italic (optional)
}
